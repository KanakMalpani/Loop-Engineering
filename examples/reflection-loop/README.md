# Reflection Loop Example

Runnable demo of a Level-2 reflective loop using the generic runtime.

```bash
cd examples/reflection-loop
python run.py
```

Uses mock LLM (no API key). Passes on iteration 3.

→ [Reflection Pattern](../../patterns/reflection-loop.md)
