# VS Code / Cursor snippets — Loop Stack v0.5

Copy [`loop.stack.code-snippets`](./loop.stack.code-snippets) into:

- **VS Code:** `.vscode/loop.stack.code-snippets` in your project, or user snippets
- **Cursor:** same path; reload window after add

## Snippets

| Prefix | Command |
|--------|---------|
| `loop combine` | Library merge → flat YAML + JSON |
| `loop quick library` | Quick path with `--library` |
| `@dev-agent` | `loop mix dev-agent` recipe |
| `@library` | Combine two library templates |
| `loopforge minjson` | LSS-min JSON export |
| `loop combine budget` | Combine with `--max-tokens` |

MCP tool descriptors: [`../mcp/loop-stack-tools.json`](../mcp/loop-stack-tools.json)
